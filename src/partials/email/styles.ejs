<%
background_color = '#fff';
text_color = '#333';
text_secondary_color = '#555';
primary_color = '#f79433';
primary_color_hover = '#f9ae64';
secondary_color = '#3b3d40';
tertiary_color = '#eff0f1';
padding_vertical = 20;
padding_horizontal = 15;
max_width = 600;
logo_width = 220;

padding_default = `padding:${padding_vertical / 2}px ${padding_horizontal}px;`;
padding_large = `padding: ${padding_vertical}px ${padding_horizontal}px;`;
font_family = 'font-family:Roboto,Helvetica Neue,sans-serif;';
font_size_sml = `font-size:12px;`;
font_size = `font-size:16px;`;
font_size_med = `font-size:18px;line-height:1.75em;`;
font_size_lrg = `font-size:20px;line-height:1.5em;`;

spacer_style = `padding-top:${padding_vertical}px;margin-top:0;margin-bottom:0;`

body_style = `${font_size}color:${text_color};${font_family}line-height:1.5em;text-align:left;`;
text_style = `${font_size}color:${text_color};${font_family}${padding_default};padding-bottom: 0;line-height:1.5em;text-align:left;`;
link_style = `${font_size}color:${primary_color};${font_family}text-decoration:underline;font-weight:bold;`;
footer_style = `${font_size_sml}color:${background_color};background-color:${secondary_color};${font_family}line-height:1.5em;text-align:center;`;

callout_container_style = `padding:${padding_vertical}px 20%;background-color:${tertiary_color};`;
callout_text_style = `${font_size_med}${font_family}color:${text_secondary_color};margin: 0 0;font-style:italic;text-align:left;`

secondary_text_style = `${font_size}color:${background_color};${font_family}padding:${padding_vertical / 2}px 0;margin:0 0;line-height:1.5em;text-align:left;`;

cta_style = `${font_size}line-height:1.5em;max-width:570px;margin:0 auto;padding:${padding_vertical}px ${padding_horizontal*2}px;border-style:solid;border-width:${padding_vertical*2}px;border-right-width:0;`
cta_header = `text-transform:uppercase;`
cta_primary_style = `background-color:${primary_color};color:${text_color};border-color:${text_color};${cta_style}`;
cta_primary_content = `color:${text_color};padding-top:0;`;

cta_secondary_style = `background-color:${text_color};color:${background_color};border-color:${primary_color};${cta_style}`;
cta_secondary_content = `color:${background_color};padding-top:0;`;

button_styles = `min-width: 200px;background:${primary_color};border: 1px solid ${primary_color};${font_family}${font_size_lrg}font-weight:bold;text-decoration: none;padding:${padding_vertical / 2}px;color:${text_color};display: block; border-radius: 4px;text-align: center; `
%>